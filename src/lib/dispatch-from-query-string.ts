import { parse } from 'query-string';

export default store => {
    // const queryString = parse(location.search);

    // dispatch actions based on qs here
    // if (// check type and dispatch) {
    //     store.dispatch({ type: //add here });
    // }
};
